spring.application.name=test

# Testlerde tabloyu otomatik olu?tur/boz
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# Hata gizlemek için (Opsiyonel ama tavsiye)
spring.jpa.properties.hibernate.format_sql=true

# DataSource (E?ER Testcontainers kullan?yorsan asla buraya eklemeyeceksin!)
# Testcontainers zaten @DynamicPropertySource ile DB bilgisi verir.
